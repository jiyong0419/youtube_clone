extends base

block content
    span=errorMessage
    form(method="post") 
        input(type="password" placeholder="Now Password" name="nowPassword")
        input(type="password" placeholder="New Password" name="newPassword")
        input(type="password" placeholder="New Password Confirm" name="newPasswordConfirm")
        input(type="submit" value="Change Password")