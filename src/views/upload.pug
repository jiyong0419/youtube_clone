extends base

block content   
    h3 hello from upload
    if errorMessage 
        span=errorMessage
    form(method="post")
        input(name="title",placeholder="Title",required,minlength=1)
        input(name="description",placeholder="Description",required,maxlength=140)
        input(name="hashtags",placeholder="Hashtags, seperated by comma.",required)
        input(type="submit",value="Upload Video")